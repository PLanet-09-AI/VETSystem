@model IEnumerable<BestReg.Data.VetAppointment>

<h2>Accepted Appointments</h2>

@if (Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Appointment ID</th>
                <th>Animal ID</th>
                <th>Start Time</th>
                <th>End Time</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var appointment in Model)
            {
                <tr>
                    <td>@appointment.Id</td>
                    <td>@appointment.AnimalId</td>
                    <td>@appointment.StartTime</td>
                    <td>@appointment.EndTime</td>
                    <td>
                        <!-- Redirect to ConductCheckup action in AnimalsController -->
                        <a asp-controller="Animals" asp-action="SearchAnimal" class="btn btn-primary">Conduct Checkup</a>

                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No accepted appointments found.</p>
}
